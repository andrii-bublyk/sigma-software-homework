<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>_Layout</title>
    <link href="~/css/sitecss.css" rel="stylesheet" />
</head>
<body>
    <div>@{await Html.RenderPartialAsync("_HeaderPartial");}</div>

    <div class="site-body">
        @RenderBody()
    </div>

    <div>
        @{await Html.RenderPartialAsync("_FooterPartial");}
    </div>
</body>
</html>
