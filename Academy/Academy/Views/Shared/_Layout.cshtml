<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>_Layout</title>
</head>
<body>
    <div>@{await Html.RenderPartialAsync("_HeaderPartial");}</div>

    <div>
        @RenderBody()
    </div>

    <div>@{await Html.RenderPartialAsync("_FooterPartial");}</div>
</body>
</html>
