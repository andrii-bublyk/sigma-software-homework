@{
    ViewData["Title"] = "AssignLecturers";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model Homework08.ViewModels.CourseLecturersAssignmentViewModel

<form method="post" action="~/Courses/AssignLecturers">
    <h2>Assign lectuters to course @Model.Course.Name</h2>
    @Html.HiddenFor(m => Model.Course.Id)
    <table>
        <thead>
            <tr>
                <th>Lecturer name</th>
                <th>Assigned</th>
            </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < Model.LecturersList.Count; i++)
            {
                <tr>
                    <td>@Model.LecturersList[i].Lecturer.Name</td>
                    <td>
                        @Html.CheckBoxFor(m => Model.LecturersList[i].IsAssigned)
                    </td>
                    @Html.HiddenFor(m => Model.LecturersList[i].Lecturer.Id)
                </tr>
            }
        </tbody>
    </table>
    <input type="submit" value="Save" />
</form>

<a href="/Courses">Back to courses list</a>